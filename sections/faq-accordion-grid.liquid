{%- style -%}
  .faq-grid-{{ section.id }} {
    padding-top: 8rem;
    padding-bottom: 8rem;
  }
{%- endstyle -%}

<section class="faq-grid faq-grid-{{ section.id }} color-{{ section.settings.color_scheme }} gradient">
  <div class="faq-grid__inner page-width">
    <ul class="faq-grid__list" role="list">
      {%- if section.blocks.size > 0 -%}
        {%- assign faq_item_index = 0 -%}
        {%- for block in section.blocks -%}
          {%- if block.type == 'section_heading' -%}
            <li class="faq-grid__section-heading" {{ block.shopify_attributes }}>
              <h2 class="faq-grid__section-title">{{ block.settings.title | escape }}</h2>
            </li>
          {%- elsif block.type == 'item' -%}
            {%- assign faq_item_index = faq_item_index | plus: 1 -%}
            {%- assign item_id = 'faq-item-' | append: section.id | append: '-' | append: faq_item_index -%}
            <li class="faq-grid__item" {{ block.shopify_attributes }}>
              <input
                id="{{ item_id }}"
                class="faq-grid__input"
                type="radio"
                name="faq-{{ section.id }}"
                {% if section.settings.open_first and faq_item_index == 1 %}
                  checked
                {% endif %}
              >

              <label for="{{ item_id }}" class="faq-grid__question-row">
                <p class="faq-grid__question">{{ block.settings.question | escape }}</p>
                <svg
                  class="faq-grid__icon faq-grid__icon--chevron"
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 20 20"
                  fill="none"
                  aria-hidden="true"
                >
                  <path
                    d="M5 8l5 5 5-5"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
              </label>

              <div class="faq-grid__answer-wrap">
                <div class="faq-grid__answer rte">
                  {{ block.settings.answer | escape | newline_to_br }}
                </div>
              </div>
            </li>
          {%- endif -%}
        {%- endfor -%}
      {%- else -%}
        {%- render 'shared-faq-content', section: section -%}
      {%- endif -%}
    </ul>
  </div>
</section>

<style>
  /* Inner uses .page-width: inherit global max-width (1440px) + horizontal padding from base.css */
  .faq-grid__heading {
    margin: 0 0 2rem;
    font-family: var(--font-heading-family);
    font-size: clamp(1.4rem, 4vw, 2.2rem);
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgb(var(--color-foreground));
    text-align: center;
  }
  .faq-grid__list {
    margin: 0;
    padding: 2rem 0;
    list-style: none;
  }
  .faq-grid__section-heading {
    list-style: none;
    margin: 0;
    padding: 2.5rem 0 1rem;
    border-bottom: none;
  }
  .faq-grid__section-heading:first-child {
    padding-top: 0;
  }
  .faq-grid__section-title {
    margin: 0;
    font-family: var(--font-heading-family);
    font-size: clamp(1rem, 2.5vw, 1.15rem);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgb(var(--color-foreground));
  }
  .faq-grid__item {
    border-bottom: 0.1rem solid rgba(var(--color-foreground), 0.2);
  }
  .faq-grid__input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }
  .faq-grid__question-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
    width: 100%;
    cursor: pointer;
    padding: 2rem 0;
  }
  .faq-grid__question {
    margin: 0;
    font-size: 1.05rem;
    color: rgb(var(--color-foreground));
  }
  .faq-grid__icon {
    width: 2rem;
    height: 2rem;
    color: rgb(var(--color-foreground));
    flex-shrink: 0;
  }
  .faq-grid__icon--chevron {
    color: rgb(var(--color-button));
    transition: transform 0.3s ease;
  }
  .faq-grid__input:checked + .faq-grid__question-row .faq-grid__icon--chevron {
    transform: rotate(180deg);
  }
  .faq-grid__answer-wrap {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows 0.3s ease;
  }
  .faq-grid__answer {
    overflow: hidden;
    padding-right: 5rem;
    color: rgba(var(--color-foreground), 0.8);
    line-height: 1.7;
  }
  .faq-grid__answer p {
    margin: 0 0 1.2rem;
  }
  .faq-grid__input:checked ~ .faq-grid__answer-wrap {
    grid-template-rows: 1fr;
  }
  .faq-grid__input:checked ~ .faq-grid__answer-wrap .faq-grid__answer {
    padding-bottom: 2rem;
  }
</style>

{% schema %}
{
  "name": "FAQ accordion grid",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Frequently asked questions"
    },
    {
      "type": "checkbox",
      "id": "open_first",
      "label": "Open first item by default",
      "default": false
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "Default FAQ visibility"
    },
    {
      "type": "checkbox",
      "id": "show_offering_heading",
      "label": "Show OUR OFFERING heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_offering_q1",
      "label": "Show: Couture vs Ready-to-Wear",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_offering_q2",
      "label": "Show: Shop both brands on website",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_offering_q3",
      "label": "Show: Do you offer custom designs",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_ordering_heading",
      "label": "Show ORDERING & PRODUCTION heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_ordering_q1",
      "label": "Show: How do I place an order",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_ordering_q2",
      "label": "Show: How long does production take",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_ordering_q3",
      "label": "Show: Adjustments to ready-to-wear",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_sizing_heading",
      "label": "Show SIZING & FIT heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_sizing_q1",
      "label": "Show: How do I choose my size",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_sizing_q2",
      "label": "Show: Made-to-measure for ready-to-wear",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_shipping_heading",
      "label": "Show SHIPPING & DELIVERY heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_shipping_q1",
      "label": "Show: Do you ship internationally",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_shipping_q2",
      "label": "Show: How long does delivery take",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_shipping_q3",
      "label": "Show: How much is shipping",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_returns_heading",
      "label": "Show RETURNS & EXCHANGES heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_returns_q1",
      "label": "Show: Do you accept returns or exchanges",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_returns_q2",
      "label": "Show: What if my item does not fit",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_returns_q3",
      "label": "Show: What payment methods do you accept",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_returns_q4",
      "label": "Show: Pay in installments",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_service_heading",
      "label": "Show SERVICE & STYLING heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_service_q1",
      "label": "Show: Speak to someone before ordering",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_service_q2",
      "label": "Show: Do you offer styling guidance",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_service_q3",
      "label": "Show: How do I care for my garment",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_craft_heading",
      "label": "Show OUR CRAFT heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_craft_q1",
      "label": "Show: Where are your pieces made",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_craft_q2",
      "label": "Show: What makes your brand different",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "section_heading",
      "name": "Section heading",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        }
      ]
    },
    {
      "type": "item",
      "name": "FAQ item",
      "settings": [
        {
          "type": "text",
          "id": "question",
          "label": "Question"
        },
        {
          "type": "textarea",
          "id": "answer",
          "label": "Answer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "FAQ accordion grid",
      "blocks": [{ "type": "item" }, { "type": "item" }, { "type": "item" }]
    }
  ]
}
{% endschema %}
